# ACoreX Component & Package Catalog

Generated from 368 documentation blueprints. Use `knowledge/index.json` for full search.

When implementing UI, read the blueprint JSON for the component before writing code.

## components (92)

- **Accordion** (`accordion`) — import { AXAccordionGroupComponent, AXAccordionItemComponent } from '@acorex/components/accordion';
  The Accordion component provides a collapsible content panel UI, supporting single or multiple open items, custom header…
- **Action Sheet** (`action-sheet`) — import { AXActionSheetComponent } from '@acorex/components/action-sheet';
  Action sheet is a type of alert offering two or more options relevant to the context. It includes a title, an optional m…
- **Alert** (`alert`) — import { AXAlertComponent } from '@acorex/components/alert';
  The Alert component displays important messages to users. It supports different types (primary, success, warning, danger…
- **Aspect Ratio** (`aspect-ratio`) — import { AXAspectRatioComponent } from '@acorex/components/aspect-ratio';
  A component that maintains a specific aspect ratio for its content, useful for responsive layouts and consistent sizing.
- **Autocomplete** (`autocomplete`) — import { AXAutocompleteComponent } from '@acorex/components/autocomplete';
  A dropdown suggestion component that filters items as the user types. Supports data sources, templates, single/multiple …
- **Avatar** (`avatar`) — import { AXAvatarComponent } from '@acorex/components/avatar';
  The Avatar component displays a user's profile image or initials, typically used for user identification. It supports mu…
- **Badge** (`badge`) — import { AXBadgeComponent } from '@acorex/components/badge';
  Badges are small status descriptors or counts used for quick recognition of an item's state or to display notifications.…
- **Bottom Navigation** (`bottom-navigation`) — import { AXBottomNavigationComponent } from '@acorex/components/bottom-navigation';
  The Bottom Navigation component provides a navigational bar at the bottom of the screen with customizable appearance and…
- **Breadcrumb** (`breadcrumb`) — import { AXBreadCrumbsComponent } from '@acorex/components/breadcrumbs';
  The Breadcrumbs component displays a navigation trail of links, helping users track their location within an application…
- **Button** (`button`) — import { AXButtonComponent } from '@acorex/components/button';
  The Button is a component which detects user interaction and triggers a corresponding event. It provides a clickable int…
- **Button Group** (`button-group`) — import { AXButtonGroupComponent } from '@acorex/components/button-group';
  Groups multiple buttons together with consistent styling and optional selection behavior. Supports color/look theming, d…
- **Button Item List** (`button-item-list`) — import { AXButtonItemListComponent } from '@acorex/components/button';
  A comprehensive component system for rendering lists of button items with various interaction states, styling options, a…
- **Calendar** (`calendar`) — import { AXCalendarComponent } from '@acorex/components/calendar';
  Calendar component enables users to view and interact with dates in a structured format. It includes features such as da…
- **Calendar Range** (`calendar-range`) — import { AXCalendarRangeComponent } from '@acorex/components/calendar';
  Calendar Range component enables users to select a range of dates using two synchronized calendar views. It provides an …
- **Checkbox** (`check-box`) — import { AXCheckBoxComponent } from '@acorex/components/check-box';
  Checkbox component offers various features to enhance user interaction and understanding. It supports disabled and reado…
- **Chips** (`chips`) — import { AXChipsComponent } from '@acorex/components/chips';
  Chips component is a versatile UI element that offers an elegant and flexible way to display and manage small pieces of …
- **Circular Progress** (`circular-progress`) — import { AXCircularProgressComponent } from '@acorex/components/circular-progress';
  Circular progress visually represents the percentage of an operation's progress within a circular format. It features cu…
- **Color Box** (`color-box`) — import { AXColorBoxComponent } from '@acorex/components/color-box';
  The Color Box component is a tool for selecting and displaying colors, featuring a popover color palette and support for…
- **Color Palette** (`color-palette`) — import { AXColorPaletteComponent } from '@acorex/components/color-palette';
  The Color Palette component enables selecting colors using preview, picker, swatches, and input sub-components. Supports…
- **Command** (`command`) — import { AXCommandComponent } from '@acorex/components/command';
  The Command Component provides a lightweight, fast, and accessible command palette interface for searching and executing…
- **Comment** (`comment`) — import { AXCommentModule } from '@acorex/components/comment';
  Comment component is an interactive element for displaying and managing user comments on posts. It's essential for socia…
- **Context Menu** (`context-menu`) — import { AXContextMenuComponent } from '@acorex/components/menu';
  The Context Menu component displays a customizable menu when a user interacts with a designated target element (typicall…
- **Conversation** (`conversation`) — import { AXChatModule } from '@acorex/components/chat';
  Conversation component enables chat-like interactions, providing an input area for users to type messages and a view sec…
- **Cron Job** (`cron-job`) — import { AXCronJobContainerComponent } from '@acorex/components/cron-job';
  The Cron Job component provides a user interface for building and editing cron expressions, allowing users to schedule r…
- **Data Pager** (`data-pager`) — import { AXDataPagerModule } from '@acorex/components/data-pager';
  The Data Pager component provides a flexible and customizable pagination UI for navigating large datasets. It supports f…
- **Data Table** (`data-table`) — import { AXDataTableModule } from '@acorex/components/data-table';
  Data Table component is a key UI element for displaying tabular data in a clear, organized format. It allows users to ef…
- **Date & Time Box** (`datetime-box`) — import { AXDateTimeBoxComponent } from '@acorex/components/datetime-box';
  Date & Time Box component enables users to input both date and time, displaying the selected values in various formats. …
- **Date & Time Picker** (`datetime-picker`) — import { AXDateTimePickerComponent } from '@acorex/components/datetime-picker';
  Date & Time Picker allows users to select date and time through an intuitive input field and visual calendar. It support…
- **Decorators** (`decorators`) — import { AXDecoratorModule } from '@acorex/components/decorators';
  A collection of utility components that provide visual enhancements and functional decorations for other UI components. …
- **Dialog** (`dialog`) — import { AXDialogComponent } from '@acorex/components/dialog';
  Dialog component is a flexible UI element for displaying information, prompts, or actions to users in a focused way. It …
- **Drawer** (`drawer`) — import { AXDrawer2Component  } from '@acorex/components/drawer-2';
  Drawer component is a sliding panel that emerges from the side of the screen, offering additional content or navigation …
- **Dropdown** (`dropdown`) — import { AXDropdownBoxComponent, AXDropdownPanelComponent } from '@acorex/components/dropdown';
  A comprehensive dropdown system consisting of a dropdown box component for triggering dropdowns and a dropdown panel com…
- **Dropdown Button** (`dropdown-button`) — import { AXDropdownButtonComponent } from '@acorex/components/dropdown-button';
  Dropdown Button enables you to seamlessly combine a button and a dropdown into one unified element. It allows users to t…
- **Editor** (`editor`) — import { AXEditorComponent } from '@acorex/components/editor';
  A rich text editor component built on EditorJS that provides a block-based editing experience with various tools for cre…
- **Floating Action Button (FAB)** (`fab`) — import { AXFabComponent } from '@acorex/components/fab';
  The Floating Action Button (FAB) highlights the primary action of a screen. It appears as a circular, elevated button th…
- **Flow Chart** (`flow-chart`) — import { AXFlowChartModule } from '@acorex/components/flow-chart';
  The Flow Chart component provides an interactive way to create, edit and visualize flow diagrams with nodes and connecti…
- **Form** (`form`) — import { AXFormModule } from '@acorex/components/form';
  Form component serves as a powerful tool for creating dynamic and responsive forms in Angular applications. With support…
- **Grid Layout Builder** (`grid-layout-builder`) — import { AXGridLayoutContainerComponent } from '@acorex/components/grid-layout-builder';
  A powerful grid layout builder component that allows users to create and manage dynamic grid layouts with draggable and …
- **Image** (`image`) — import { AXImageComponent } from '@acorex/components/image';
  Image component blends efficiency and aesthetics, offering lazy and priority loading for optimized performance. It featu…
- **Image Editor** (`image-editor`) — import { AXImageEditorModule } from '@acorex/components/image-editor';
  Image Editor component provides users with a comprehensive set of tools to modify and enhance images. Features such as c…
- **JSON Viewer** (`json-viewer`) — import { JsonViewerComponent } from '@acorex/components/json-viewer';
  A component that provides a visual representation of JSON data with collapsible sections, syntax highlighting, and suppo…
- **Kanban** (`kanban`) — import { AXKanbanComponent } from '@acorex/components/kanban';
  Kanban component displays items grouped by statuses with drag-and-drop reordering and cross-status movement. It supports…
- **Keyboard** (`kbd`) — import { AXKBDComponent } from '@acorex/components/kbd';
  Displays keyboard keys and shortcuts. Use <code>ax-kbd</code> as a container and <code>ax-kbd-item</code> to render sing…
- **Label** (`label`) — import { AXLabelComponent } from '@acorex/components/label';
  A label component that provides accessible text labels for form controls and other UI elements. Labels can be associated…
- **List** (`list`) — import { AXListComponent } from '@acorex/components/list';
  Displays a virtualized, focusable list with single/multiple selection, custom item/header/footer templates, and loading/…
- **Loading** (`loading`) — import { AXLoadingComponent } from '@acorex/components/loading';
  Displays a loading indicator. Supports embedding a spinner inline or showing a modal overlay via service. Inherits core …
- **Loading Dialog** (`loading-dialog`) — import { AXLoadingDialogComponent } from '@acorex/components/loading-dialog';
  Displays a modal dialog with a progress indicator and optional actions. Use it to inform users about the status of long-…
- **Map** (`map`) — import { AXMapComponent } from '@acorex/components/map';
  Map provides an interactive map powered by Leaflet. It supports markers, location tracking, and configurable zoom, latit…
- **Media Viewer** (`media-viewer`) — import { AXMediaViewerModule } from '@acorex/components/media-viewer';
  A component that provides a rich media viewing experience with support for images, videos, and documents. It includes fe…
- **Menu** (`menu`) — import { AXMenuComponent } from '@acorex/components/menu';
  Displays a hierarchical action list with support for nested items, icons, and decorators. Supports horizontal and vertic…
- **Modal** (`modal`) — import { AXModalComponent } from '@acorex/components/modal';
  A comprehensive modal dialog system that provides programmatic and declarative ways to display content in overlay dialog…
- **Navbar** (`navbar`) — import { AXNavbarComponent } from '@acorex/components/navbar';
  A navigation bar component that supports custom styling and encapsulation. It provides a consistent way to display navig…
- **Notification** (`notification`) — import { AXNotificationComponent } from '@acorex/components/notification';
  A component that displays toast notifications with support for different types (success, error, warning, info), custom c…
- **Number Box** (`number-box`) — import { AXNumberBoxModule } from '@acorex/components/number-box';
  A component that provides a numeric input field with increment and decrement controls. It supports min/max values, step …
- **OTP** (`otp`) — import { AXOtpComponent } from '@acorex/components/otp';
  A component for OTP input fields with state management and custom styling support. Provides multiple input fields for en…
- **Paint** (`paint`) — import { AXPaintModule } from '@acorex/components/paint';
  A canvas-based drawing component that provides a comprehensive painting interface with multiple drawing tools, color cus…
- **Password Box** (`password-box`) — import { AXPasswordBoxComponent } from '@acorex/components/password-box';
  A specialized input field designed for securely capturing user passwords. It incorporates essential features to enhance …
- **Phone Box** (`phone-box`) — import { AXPhoneBoxComponent } from '@acorex/components/phone-box';
  A component that provides a phone number input field with support for country selection, formatting, and validation. Thi…
- **Picker** (`picker`) — import { AXPickerModule } from '@acorex/components/picker';
  A component that provides a customizable picker interface for selecting values from a predefined range. It provides an i…
- **Popover** (`popover`) — import { AXPopoverComponent } from '@acorex/components/popover';
  A component that displays a floating panel containing additional content or controls. It can be positioned relative to a…
- **Popup** (`popup`) — import { AXPopupComponent ,AXPopupService } from '@acorex/components/popup';
  A dialog-like component that renders provided content in an overlay window. Opened via service with configurable size, h…
- **Progress Bar** (`progress-bar`) — import { AXProgressBarComponent } from '@acorex/components/progress-bar';
  A component that displays the progress of a task or operation. It supports different types (determinate/indeterminate), …
- **QR Code** (`qrcode`) — import { AXQrcodeComponent } from '@acorex/components/qrcode';
  A component that generates and displays QR codes. It supports size, error correction levels, colors, and multiple output…
- **Query Builder** (`query-builder`) — import { AXQueryBuilderComponent } from '@acorex/components/query-builder';
  A component that provides a visual interface for building structured filters using groups and rules, emitting normalized…
- **Rail Navigation** (`rail-navigation`) — import { AXRailNavigationComponent } from '@acorex/components/rail-navigation';
  A vertical navigation rail with multiple visual looks and start/end locations. Supports active/disabled items and option…
- **Range Slider** (`range-slider`) — import { AXRangeSliderComponent } from '@acorex/components/range-slider';
  A range slider with single/dual modes, horizontal/vertical orientations, step and snap options, and optional tooltips/la…
- **Rate Picker** (`rate-picker`) — import { AXRatePickerComponent } from '@acorex/components/rate-picker';
  An interactive rating component with customizable icon, max value, step precision and smooth transitions. Extends MXValu…
- **Rest API Generator** (`rest-api-generator`) — import { AXRestApiGeneratorComponent } from '@acorex/components/rest-api-generator';
  A component that assembles REST request configuration (URL, method, headers, auth, query params and body) and emits a no…
- **Result** (`result`) — import { AXResultComponent } from '@acorex/components/result';
  Displays a result view with icon, title and description using content projection.
- **RRule** (`rrule`) — import { AXRruleComponent } from '@acorex/components/rrule';
  A powerful recurrence rule component for creating and managing complex scheduling patterns with support for various freq…
- **Scheduler** (`scheduler`) — import { AXSchedulerComponent } from '@acorex/components/scheduler';
  A full-featured scheduler/calendar component supporting multiple views, drag & drop, data loading, holidays, and localiz…
- **Search Box** (`search-box`) — import { AXSearchBoxComponent } from '@acorex/components/search-box';
  An input component for searching text with debounce, clear button, customizable looks and sizes.
- **Select Box** (`select-box`) — import { AXSelectBoxComponent } from '@acorex/components/select-box';
  A dropdown selection component with data source support, search integration, single/multiple selection, templates and lo…
- **Selection List** (`selection-list`) — import { AXSelectionListComponent } from '@acorex/components/selection-list';
  Presents a list of selectable items with single/multiple modes, configurable direction and looks, optional controls, cus…
- **Side Menu** (`side-menu`) — import { AXSideMenuComponent } from '@acorex/components/side-menu';
  A sidebar navigation menu with nested items, configurable looks and indicator location, decorators support, and item cli…
- **Skeleton** (`skeleton`) — import { AXSkeletonComponent } from '@acorex/components/skeleton';
  A component that displays a placeholder loading state with animated shimmer effect, commonly used to indicate content is…
- **Sliding Item** (`sliding-item`) — import { AXSlidingItemModule } from '@acorex/components/sliding-item';
  A component that provides a swipeable item with revealable action buttons, commonly used in lists to show additional act…
- **Step Wizard** (`step-wizard`) — import { AXStepWizardComponent } from '@acorex/components/step-wizard';
  A step-by-step wizard component for guiding users through multi-step processes
- **Switch** (`switch`) — import { AXSwitchComponent } from '@acorex/components/switch';
  A component that provides a toggle switch control, allowing users to enable or disable a setting with a sliding animatio…
- **Tabs** (`tabs`) — import { AXTabsComponent } from '@acorex/components/tabs';
  A component that provides a tabbed interface, allowing users to switch between different views or sections of content.
- **Tag** (`tag`) — import { AXTagComponent } from '@acorex/components/tag';
  Tag component is a UI element used to represent labels or categories, displayed as small, styled text or icons. It helps…
- **Tag Box** (`tag-box`) — import { AXTagBoxComponent } from '@acorex/components/tag-box';
  A component that provides a container for displaying and managing multiple tags, with support for adding, removing, and …
- **Text Area** (`text-area`) — import { AXTextAreaComponent } from '@acorex/components/text-area';
  A component that provides a multi-line text input field with support for resizing, validation, and custom styling.
- **Text Box** (`text-box`) — import { AXTextBoxComponent } from '@acorex/components/text-box';
  A component that provides a single-line text input field with various features like validation, masking, and formatting.
- **Time Duration** (`time-duration`) — import { AXTimeDurationComponent } from '@acorex/components/time-duration';
  A component that provides a time duration input field with support for various time units (from milliseconds to years), …
- **Time Line** (`time-line`) — import { AXTimeLineComponent } from '@acorex/components/time-line';
  A component that displays a sequence of events or steps in chronological order, with support for custom icons, status in…
- **Toast** (`toast`) — import { AXToastComponent } from '@acorex/components/toast';
  A component that displays temporary notifications or messages to users, with support for different types, auto-dismissal…
- **Toolbar** (`toolbar`) — import { AXToolBarComponent } from '@acorex/components/toolbar';
  The Toolbar component provides a horizontal container for grouping buttons, decorators, and content in a compact layout.
- **Tooltip** (`tooltip`) — import { AXTooltipComponent } from '@acorex/components/tooltip';
  A component and directive that displays additional information when hovering over or focusing on an element, with suppor…
- **Tree View** (`tree-view`) — import { AXTreeViewComponent } from '@acorex/components/tree-view';
  The Tree View component displays hierarchical data in a tree structure with support for selection, expansion, and custom…
- **Uploader** (`uploader`) — import { AXUploaderDropZoneComponent } from '@acorex/components/uploader';
  The Uploader component provides a flexible interface for uploading files, supporting drag-and-drop, file lists, and dial…
- **WYSIWYG** (`wysiwyg`) — import { AXWysiwygModule } from '@acorex/components/wysiwyg';
  WYSIWYG component is a rich text editor that enables users to create and format content with a user-friendly interface. …

## cdk (14)

- **Accordion CDK** (`accordion`) — import { AXAccordionCdkModule } from '@acorex/cdk/accordion';
  The Accordion CDK provides a set of directives for creating collapsible content sections with smooth animations. It cons…
- **Carousel Directive** (`carousel`) — import { AXCarouselDirective } from '@acorex/cdk/carousel';
  Integrates Swiper.js into ACoreX applications to create highly customizable and responsive carousels/sliders. The direct…
- **Drag and Drop** (`drag-drop`) — import { AXDragDirective } from '@acorex/cdk/drag-drop';
  A flexible set of directives for implementing drag-and-drop functionality in your Angular applications. The drag-drop sy…
- **Drawer** (`drawer`) — import { AXDrawerDirectiveModule } from '@acorex/cdk/drawer';
  Drawer directive provides a simple way to integrate an off-canvas panel. To use it, apply axDrawerContainer to the paren…
- **Focus Trap** (`focus-trap`) — import { AXFocusTrapDirective } from '@acorex/cdk/focus-trap';
  A directive that traps keyboard focus within a specific DOM element, ensuring that Tab and Shift+Tab navigation remains …
- **Input Mask** (`input-mask`) — import { AXInputMaskDirective } from '@acorex/cdk/input-mask';
  A powerful Angular directive that provides input masking functionality for various data types including Hijri dates, dig…
- **List Navigation** (`list-navigation`) — import { AXListNavigationModule } from '@acorex/cdk/list-navigation';
  A directive that facilitates keyboard navigation within lists. The parent element uses the axListNavigation directive, a…
- **Outline** (`outline`) — import { AXOutlineContainerDirective } from '@acorex/cdk/outline';
  The ACoreX outline directives provide a robust and streamlined way to create dynamic, scroll-linked navigation menus wit…
- **Pan View** (`pan-view`) — import { AXPanViewDirective } from '@acorex/cdk/pan-view';
  The AXPanViewDirective provides a way to implement pan and zoom functionality for a designated content area within your …
- **Selection** (`selection`) — import { AXSelectionCdkModule } from '@acorex/cdk/selection';
  The selection CDK provides directives for creating interactive selection functionality in lists and collections. It supp…
- **Sliding Item** (`sliding-item`) — import { AXSlidingItemDirective } from '@acorex/cdk/sliding-item';
  A directive that enables horizontal sliding functionality for elements. It provides interactive drag-to-slide behavior w…
- **Uploader** (`uploader`) — import { AXUploaderZoneDirective } from '@acorex/cdk/uploader';
  The Uploader CDK provides directives and services for file upload functionality, including drag-and-drop support, file b…
- **Virtual Scroll** (`virtual-scroll`) — import { AXVirtualScrollModule } from '@acorex/cdk/virtual-scroll';
  Virtual scrolling directive efficiently renders large datasets by only displaying items visible in the viewport. It requ…
- **WYSIWYG** (`wysiwyg`) — import { AXWysiwyg } from '@acorex/cdk/wysiwyg';
  The AXWysiwyg class is a wrapper around the Quill rich text editor, designed to simplify its usage in ACorex library. Th…

## core (18)

- **Browser Storage Services** (`storage`) — import { AXLocalStorageService, AXSessionStorageService, AXCookieStorageService, AX_LOCALSTORAGE_SECRET_KEY, AXStorage, AXCookieOptions } from '@acorex/core/storage';
  SSR-safe wrappers for localStorage, sessionStorage, and cookies: JSON persistence, optional AES encryption for localStor…
- **Component Service** (`components`) — import { AXComponentService, AXComponentType, AXComponentContent, AXComponentInputs, AXComponentOptions } from '@acorex/core/components';
  A root-level utility service for dynamically creating Angular components and embedded views from templates at runtime. I…
- **Constants** (`constants`) — import { COUNTRIES, COUNTRIES_FLAG_BASE64, CountryItem } from '@acorex/core/constants';
  Shared geographic constants for AcoreX. Provides a typed country dataset (names, regions, ISO codes, dialing codes, phon…
- **Date & Time** (`date-time`) — import { AXCalendarService, AXDateTime, AXDateValue, AXDateTimeRange } from '@acorex/core/date-time';
  Calendar abstraction and date/time utilities for AcoreX. Supports Gregorian and Solar Hijri calendars, locale-aware form…
- **Event Service** (`events`) — import { AXEventService, AXSystemEvents, AXEventData } from '@acorex/core/events';
  A root-level service for application-wide communication using two complementary patterns: an RxJS Observable stream (emi…
- **File Utilities** (`file`) — import { AXFileService, AXFileModule, AXFileSizeFormatter } from '@acorex/core/file';
  Browser-side file utilities: programmatic file selection via a hidden input, Blob-to-Base64 conversion, base64 size calc…
- **Format System** (`format`) — import { AXFormatService, AXFormatModule, AXFormatPipe, AXFormatterDirective, AXFormatter, AXFormatterRegistryService, AXNumberFormatter, AXStringFormatter } from '@acorex/core/format';
  A pluggable Angular formatting framework: registry-based formatters, built-in number and string formatters, reactive for…
- **Icon Resolver** (`icon`) — import { AXIconResolverService, AXIconResolver, AX_ICON_RESOLVER, AXIconResolverDefault } from '@acorex/core/icon';
  A pluggable icon resolution system: register custom AXIconResolver implementations via dependency injection, resolve ico…
- **Image Service** (`image`) — import { AXImageService } from '@acorex/core/image';
  Browser-side image resizing via canvas: scale images from File or HTMLImageElement sources down to a max dimension while…
- **Locale System** (`locale`) — import { AXLocaleService, AXLocaleProfileProviderService, AX_LOCALE_CONFIG, AX_LOCALE_PROFILE_PROVIDERS, AXLocaleProfile, AXLocaleProfileProvider, AXUSLocaleProfile, AXIRLocaleProfile, AXAULocaleProfile } from '@acorex/core/locale';
  Locale profile management for AcoreX: lazy-loaded regional settings (date/time formats, calendar, units, RTL), reactive …
- **Memoize Decorator** (`memoize`) — import { Memoize } from '@acorex/core/memoize';
  A method decorator that caches synchronous method results using memoizee. Repeated calls with the same arguments return …
- **Network Service** (`network`) — import { AXNetworkService, AXUploadTask, AXDownloadTask, AXUploadStatus, AXDownloadStatus, AXUploadResponse, AXDownloadResponse } from '@acorex/core/network';
  Browser-aware network utilities: online/offline status signal, file uploads and downloads with progress tracking, cancel…
- **Platform Service** (`platform`) — import { AXPlatform, AXThemeMode, AXPlatformEvent, isBrowser, isServer, PLATFORMS_MAP } from '@acorex/core/platform';
  Runtime environment detection and application chrome utilities: device/browser/screen checks, dark/light theme managemen…
- **RRule Service** (`rrule-service`) — import { AXRruleService } from '@acorex/components/rrule';
  Service for working with RRule recurrence patterns. Provides methods to create RRule instances from options or strings, …
- **Safe Pipe** (`pipes`) — import { AXSafePipe } from '@acorex/core/pipes';
  Angular pipe that wraps DomSanitizer to mark trusted HTML, styles, scripts, URLs, and resource URLs as safe for template…
- **Translation Service** (`translation`) — import { AXTranslationService, AXTranslationModule, AXTranslatorPipe, AXTranslatorDirective, AXTranslationLoaderService, AX_TRANSLATION_CONFIG, AX_TRANSLATION_LOADER, translationConfig, loadTranslationScope, translateSync, createMultiLanguageString, AXMultiLanguageMap, AXTranslateOptions } from '@acorex/core/translation';
  Multi-language i18n for Angular: load JSON translation files by lang and scope, cache them, resolve nested keys and inli…
- **Utility Library** (`utils`) — import { waitFor, delay, retryUntil, pollUntil, pollUntilAbortable, throttleAsync, debounceAsync, fallbackChain, AXHtmlUtil, AXDrawingUtil, AXColorUtil, AXUnsubscriber, AXAutoUnsubscriber, getWordBoundsAtPosition, AXPoint, AXBoundingClientRect } from '@acorex/core/utils';
  Stateless helper functions and static utility classes for async control, DOM/focus management, RxJS subscription cleanup…
- **Validation System** (`validation`) — import { AXValidationService, AXValidationRegistryService, AXValidationModule, AX_VALIDATION_CONFIG, validationConfig, AXValidationRule, AXValidationRuleResult, AXValidationSummary, AXRequiredValidationRule, AXRegexValidationRule, AXCallbackValidationRule } from '@acorex/core/validation';
  Pluggable async validation framework: register built-in and custom rules, define named aliases (email, phone), integrate…

## charts (6)

- **Bar Chart** (`bar-chart`) — import { AXBarChartComponent } from '@acorex/charts/bar-chart';
  A powerful and customizable bar chart component for displaying categorical data with interactive features, animations, a…
- **Chart Legend** (`chart-legend`) — import { AXChartLegendComponent } from '@acorex/charts/chart-legend';
  An interactive legend component that connects to charts for enhanced user experience, supporting both vertical and horiz…
- **Donut Chart** (`donut-chart`) — import { AXDonutChartComponent } from '@acorex/charts/donut-chart';
  An interactive donut chart component for visualizing proportional data and part-to-whole relationships with customizable…
- **Gauge Chart** (`gauge-chart`) — import { AXGaugeChartComponent } from '@acorex/charts/gauge-chart';
  A gauge chart component for visualizing progress, metrics, and KPIs with customizable thresholds, animations, and respon…
- **Hierarchy Chart** (`hierarchy-chart`) — import { AXHierarchyChartComponent } from '@acorex/charts/hierarchy-chart';
  An interactive hierarchy chart component for visualizing organizational structures, family trees, and hierarchical relat…
- **Line Chart** (`line-chart`) — import { AXLineChartComponent } from '@acorex/charts/line-chart';
  An interactive line chart component for visualizing trends, time series data, and continuous data with smooth animations…

## types (166)

- **Action Sheet Configuration** (`ax-action-sheet-config`) — import { AXActionSheetConfig } from '@acorex/components/action-sheet';
  Configuration interface for action sheet appearance and behavior. This interface defines all the properties that can be …
- **Action Sheet Content Type** (`ax-action-sheet-content-type`) — import { AXActionSheetContentType } from '@acorex/components/action-sheet';
  Type definition for custom content that can be displayed in an action sheet. This type allows for either a template refe…
- **Action Sheet Item** (`ax-action-sheet-item`) — import { AXActionSheetItem } from '@acorex/components/action-sheet';
  Represents an individual item in an action sheet. This interface defines the structure for action sheet items, including…
- **AXBarChartData** (`bar-chart-data`) — import { AXBarChartData } from '@acorex/charts/bar-chart';
  Interface defining the structure of individual data items for bar charts
- **AXBarChartOption** (`bar-chart-option`) — import { AXBarChartOption } from '@acorex/charts/bar-chart';
  Bar chart options interface for configuring chart appearance, behavior, and functionality
- **AXBarChartValue** (`bar-chart-value`) — import { AXBarChartValue } from '@acorex/charts/bar-chart';
  Bar chart data type - represents an array of bar chart data items or clustered data groups
- **AXBrowsers** (`ax-browsers`) — import { AXBrowsers } from '@acorex/core/platform';
  The browser type
- **AXButtonItemListItem** (`button-item-list-item`) — import { AXButtonItemListItem } from '@acorex/components/button';
  Interface defining the structure of a button item within a button item list. This type is used to programmatically creat…
- **AXButtonType** (`button-type`) — import { AXButtonType } from '@acorex/components/button';
  Type definition for button types used throughout the UI library.
- **AXCalendar** (`ax-calendar`) — import { AXCalendar } from '@acorex/core/date-time';
  AXCalendar is an abstract base class that defines the interface for calendar implementations. It provides methods for da…
- **AXCalendar** (`ax-calender`) — import { AXCalendar } from '@acorex/core/date-time';
  The calendar type
- **AXCarousel** (`ax-carousel`) — import { AXCarousel } from '@acorex/cdk/carousel';
  Type alias for the Swiper instance used by the carousel directive.
- **AXCarouselOptions** (`ax-carousel-options`) — import { AXCarouselOptions } from '@acorex/cdk/carousel';
  Type alias for Swiper configuration options used by the carousel directive.
- **AXChartLegendCompatible** (`chart-legend-compatible`) — import { AXChartLegendCompatible } from '@acorex/charts/chart-legend';
  Interface that chart components should implement to work with the legend
- **AXChartLegendOptions** (`chart-legend-options`) — import { AXChartLegendOptions } from '@acorex/charts/chart-legend';
  Legend configuration options for customizing legend appearance and behavior
- **AXChipsLookType** (`ax-chips-look-type`) — import { AXChipsLookType } from '@acorex/components/chips';
  Defines the appearance styles available for chips components. This type is a subset of AXStyleLookType, specifically exc…
- **AXChipsLookType** (`chips-look-type`) — import { AXChipsLookType } from '@acorex/components/chips';
  Type definition for the allowed visual appearance styles for the Chips component. This is a subset of AXStyleLookType.
- **AXChipsSizeType** (`ax-chips-size-type`) — import { AXChipsSizeType } from '@acorex/components/chips';
  Defines the size options available for chips components. This type is a subset of AXStyleSizeType, specifically excludin…
- **AXClipBoardService** (`clipboard-service`) — import { AXClipBoardService } from '@acorex/cdk/clipboard';
  Service class for clipboard operations including copy and paste functionality.
- **AXColorMode** (`ax-color-mode`) — import { AXColorMode } from '@acorex/core/utils';
  The mode to convert the color to.
- **AXColorUtil** (`color-util`) — import { AXColorUtil } from '@acorex/core/utils';
  Utility class for color manipulation, conversion, and formatting operations.
- **AXConnectedPosition** (`connected-position`) — import { AXConnectedPosition } from '@acorex/cdk/common';
  Interface defining the positioning configuration for connected elements with precise control over origin and overlay ali…
- **AXDataSource** (`data-source`) — import { AXDataSource } from '@acorex/cdk/common';
  Generic data source class for managing data with filtering, sorting, and pagination capabilities.
- **AXDayOfWeekName** (`ax-day-of-week-name`) — import { AXDayOfWeekName } from '@acorex/components/scheduler';
  Represents the names of days of the week.
- **AXDonutChartData** (`donut-chart-data`) — import { AXDonutChartData } from '@acorex/charts/donut-chart';
  Interface representing a single segment in the donut chart with properties for identification, value, styling, and label…
- **AXDonutChartOption** (`donut-chart-option`) — import { AXDonutChartOption } from '@acorex/charts/donut-chart';
  Configuration options for the donut chart component including dimensions, styling, animations, and interactive features
- **AXDonutChartValue** (`donut-chart-value`) — import { AXDonutChartValue } from '@acorex/charts/donut-chart';
  Data structure provided to the donut chart component - represents an array of donut chart data items
- **AXDownloadStatus** (`ax-download-status`) — import { AXDownloadStatus } from '@acorex/core/network';
  The AXDownloadStatus type
- **AXDrawingUtil** (`drawing-util`) — import { AXDrawingUtil } from '@acorex/core/utils';
  Utility class for drawing operations, geometric calculations, and canvas manipulation.
- **AXExpandToggleIcons** (`expand-toggle-icons`) — import { AXExpandToggleIcons } from '@acorex/cdk/common';
  Defines custom icons for expand/collapse toggles in tree view components.
- **AXFormatter** (`ax-formatter`) — import { AXFormatter } from '@acorex/core/format';
  The AXFormatter type
- **AXGaugeChartOption** (`gauge-chart-option`) — import { AXGaugeChartOption } from '@acorex/charts/gauge-chart';
  Configuration options for the gauge chart component including dimensions, thresholds, animations, and interactive featur…
- **AXGaugeChartValue** (`gauge-chart-value`) — import { AXGaugeChartValue } from '@acorex/charts/gauge-chart';
  Gauge chart data type - represents a single numeric value for the gauge chart
- **AXHierarchyChartData** (`hierarchy-chart-data`) — import { AXHierarchyChartData } from '@acorex/charts/hierarchy-chart';
  Hierarchy chart data type - represents a single node in the hierarchy with all its properties and child relationships
- **AXHierarchyChartNodeContext** (`hierarchy-chart-node-context`) — import { AXHierarchyChartNodeContext } from '@acorex/charts/hierarchy-chart';
  Context data interface for node template rendering in hierarchy charts, providing access to node properties, expansion s…
- **AXHierarchyChartOption** (`hierarchy-chart-option`) — import { AXHierarchyChartOption } from '@acorex/charts/hierarchy-chart';
  Configuration options for the hierarchy chart component including layout, styling, animations, and interactive features
- **AXHolidayDate** (`ax-holiday-date`) — import { AXHolidayDate } from '@acorex/core/date-time';
  Represents a holiday date in the scheduler.
- **AXLengthValidationRule** (`length-validation-rule`) — import { AXLengthValidationRule } from '@acorex/core/validation';
  Validation rule class that validates the length of string values.
- **AXLocaleProfile** (`ax-localeprofile`) — import { AXLocaleProfile } from '@acorex/core/locale';
  The locale profile type
- **AXLocaleService** (`locale-service`) — import { AXLocaleService } from '@acorex/core/locale';
  Service class for managing locale-specific formatting, date/time, and number formatting.
- **AXLocation** (`location`) — import { AXLocation } from '@acorex/cdk/common';
  Type definition for positioning locations used in overlays, tooltips, and other positioned components.
- **AXMapControlPlace** (`ax-map-control-place`) — import { AXMapControlPlace } from '@acorex/components/map';
  Defines the possible positions for map controls on the map interface.
- **AXMapData** (`ax-map-data`) — import { AXMapData } from '@acorex/components/map';
  Represents a collection of map data including markers, polygons, and POI markers.
- **AXMapLatLng** (`ax-map-latlng`) — import { AXMapLatLng } from '@acorex/components/map';
  Represents a geographical coordinate point with latitude and longitude.
- **AXMapLocation** (`ax-map-location`) — import { AXMapLocation } from '@acorex/components/map';
  Represents a location on the map with additional precision information, extending AXMapMarker.
- **AXMapMarker** (`ax-map-marker`) — import { AXMapMarker } from '@acorex/components/map';
  Represents a marker on the map with position and optional display properties.
- **AXMapPolygon** (`ax-map-polygon`) — import { AXMapPolygon } from '@acorex/components/map';
  Represents a polygon on the map with points and optional styling properties.
- **AXMaxLengthValidationRule** (`max-length-validation-rule`) — import { AXMaxLengthValidationRule } from '@acorex/core/validation';
  Validation rule class that ensures a string value does not exceed a maximum length requirement.
- **AXMenuItem** (`ax-menu-item`) — import { AXMenuItem } from '@acorex/components/menu';
  Type describing a menu item object used by AXMenuComponent for declarative item rendering.
- **AXMinLengthValidationRule** (`min-length-validation-rule`) — import { AXMinLengthValidationRule } from '@acorex/core/validation';
  Validation rule class that ensures a string value meets a minimum length requirement.
- **AXModalOptions** (`modal-options`) — import { AXModalOptions } from '@acorex/components/modal';
  Configuration options for modal dialogs that extend overlay options with modal-specific features.
- **AXModalRef** (`modal-ref`) — import { AXModalRef } from '@acorex/components/modal';
  Reference object returned when opening a modal that provides methods to control the modal's behavior and state.
- **AXNetworkService** (`network-service`) — import { AXNetworkService } from '@acorex/core/network';
  Service class for network operations including upload, download, and HTTP requests.
- **AXNotificationButtonItem** (`notification-button-item`) — import { AXNotificationButtonItem } from '@acorex/components/notification';
  Configuration interface for action buttons displayed in notifications.
- **AXNotificationConfig** (`notification-config`) — import { AXNotificationConfig } from '@acorex/components/notification';
  Global configuration interface for notification service settings and defaults.
- **AXNotificationContentType** (`notification-content-type`) — import { AXNotificationContentType } from '@acorex/components/notification';
  Union type defining the allowed content types for notification content.
- **AXNotificationData** (`notification-data`) — import { AXNotificationData } from '@acorex/components/notification';
  Internal data structure used by the notification component for configuration and state management.
- **AXNotificationOptions** (`notification-options`) — import { AXNotificationOptions } from '@acorex/components/notification';
  Configuration options for creating and displaying notifications programmatically.
- **AXNotificationRef** (`notification-ref`) — import { AXNotificationRef } from '@acorex/components/notification';
  Reference interface for programmatically controlling created notifications.
- **AXOrientation** (`ax-orientation`) — import { AXOrientation } from '@acorex/cdk/common';
  Defines the available orientation options for components
- **AXOverlayService** (`overlay-service`) — import { AXOverlayService } from '@acorex/cdk/overlay';
  Service class for managing overlay components and modal dialogs.
- **AXPlacement** (`placement`) — import { AXPlacement } from '@acorex/cdk/common';
  String literal type defining all available placement options for positioning elements relative to a target.
- **AXPlacementType** (`placement-type`) — import { AXPlacementType } from '@acorex/cdk/common';
  A flexible type that accepts various placement configurations including simple strings, connected positions, or arrays o…
- **AXPlatform** (`platform`) — import { AXPlatform } from '@acorex/core/platform';
  Service class for detecting and managing platform-specific information including browser, device, and technology detecti…
- **AXPlatforms** (`ax-platforms`) — import { AXPlatforms } from '@acorex/core/platform';
  The platform type
- **AXPoiMarker** (`ax-poi-marker`) — import { AXPoiMarker } from '@acorex/components/map';
  Represents a Point of Interest marker on the map with the same properties as AXMapMarker plus additional POI-specific pr…
- **AXRailLocation** (`rail-location`) — import { AXRailLocation } from '@acorex/components/rail-navigation';
  Location options for rail navigation component placement.
- **AXRailLook** (`rail-look`) — import { AXRailLook } from '@acorex/components/rail-navigation';
  Visual appearance options for rail navigation component.
- **AXRange** (`ax-range`) — import { AXRange } from '@acorex/cdk/common';
  A simple generic interface representing a range with a start (from) and an end value.
- **AXRegexValidationRule** (`regex-validation-rule`) — import { AXRegexValidationRule } from '@acorex/core/validation';
  Validation rule class that validates values against regular expressions.
- **AXRequiredValidationRule** (`required-validation-rule`) — import { AXRequiredValidationRule } from '@acorex/core/validation';
  Validation rule class that ensures a field is not empty or null.
- **AXROptionsType** (`rrule-options`) — import { AXROptionsType } from '@acorex/components/rrule';
  Type alias for RRule options interface that provides comprehensive recurrence rule configuration options aligned with th…
- **AXRRuleType** (`rrule`) — import { AXRRuleType } from '@acorex/components/rrule';
  RRule class type representing recurrence rule instances from the rrule.js library, providing comprehensive methods for q…
- **AXSameSite** (`ax-samesite`) — import { AXSameSite } from '@acorex/core/storage';
  The same site type
- **AXSchedulerAppointmentMouseEvent** (`ax-scheduler-appointment-mouse-event`) — import { AXSchedulerAppointmentMouseEvent } from '@acorex/components/scheduler';
  Event data for appointment mouse interactions in the scheduler.
- **AXSchedulerAppointmentSegment** (`ax-scheduler-appointment-segment`) — import { AXSchedulerAppointmentSegment } from '@acorex/components/scheduler';
  Represents a segment of an appointment that spans multiple days.
- **AXSchedulerBlockIdentifier** (`ax-scheduler-block-identifier`) — import { AXSchedulerBlockIdentifier } from '@acorex/components/scheduler';
  Identifies a specific time block within a scheduler view.
- **AXSchedulerDateRange** (`ax-scheduler-date-range`) — import { AXSchedulerDateRange } from '@acorex/components/scheduler';
  Represents a date range for scheduler operations.
- **AXSchedulerSlotDropEvent** (`ax-scheduler-slot-drop-event`) — import { AXSchedulerSlotDropEvent } from '@acorex/components/scheduler';
  Event data for appointment drop operations in the scheduler.
- **AXSchedulerSlotMouseEvent** (`ax-scheduler-slot-mouse-event`) — import { AXSchedulerSlotMouseEvent } from '@acorex/components/scheduler';
  Event data for slot mouse interactions in the scheduler.
- **AXScreenSizes** (`ax-screen-sizes`) — import { AXScreenSizes } from '@acorex/core/platform';
  The screen size type
- **AXSizeType** (`size-type`) — import { AXSizeType } from '@acorex/cdk/common';
  Type definition for component size variants used throughout the UI library.
- **AXStepWizardLook** (`step-wizard-look`) — import { AXStepWizardLook } from '@acorex/components/step-wizard';
  Defines the available visual appearance options for step wizard components
- **AXStepWizardSize** (`step-wizard-size`) — import { AXStepWizardSize } from '@acorex/components/step-wizard';
  Defines the available size options for step wizard components
- **AXStepWizardState** (`step-wizard-state`) — import { AXStepWizardState } from '@acorex/components/step-wizard';
  Defines the available state options for step wizard items
- **AXStorage** (`storage`) — import { AXStorage } from '@acorex/core/storage';
  Interface for storage operations providing a common API for different storage mechanisms like localStorage, sessionStora…
- **AXStyleColorType** (`style-color-type`) — import { AXStyleColorType } from '@acorex/cdk/common';
  Predefined color schemes available for styling components.
- **AXStyleLookType** (`style-look-type`) — import { AXStyleLookType } from '@acorex/cdk/common';
  Type definition for component visual appearance styles used throughout the UI library.
- **AXSurfaceType** (`surface-type`) — import { AXSurfaceType } from '@acorex/cdk/common';
  Type definition for surface color variants used in design system theming.
- **AXTabLocation** (`tab-location`) — import { AXTabLocation } from '@acorex/components/tabs';
  Predefined location positions available for tab components.
- **AXTabLook** (`tab-look`) — import { AXTabLook } from '@acorex/components/tabs';
  Predefined look styles available for tab components.
- **AXTechnologies** (`ax-technologies`) — import { AXTechnologies } from '@acorex/core/platform';
  The technology type
- **AXThemeMode** (`ax-theme-mode`) — import { AXThemeMode } from '@acorex/core/platform';
  The theme mode
- **AXTimeLineMode** (`time-line-mode`) — import { AXTimeLineMode } from '@acorex/components/time-line';
  Defines the display mode for time line items.
- **AXTimeSpanTotal** (`ax-timespan-total`) — import { AXTimeSpanTotal } from '@acorex/core/date-time';
  The AXTimeSpanTotal type
- **AXToastData** (`toast-data`) — import { AXToastData } from '@acorex/components/toast';
  Configuration interface for toast notification display options.
- **AXTranslationService** (`translation-service`) — import { AXTranslationService } from '@acorex/core/translation';
  Service class for managing translations, loading translation files, and providing translation functionality throughout t…
- **AXTreeViewExpandBehavior** (`tree-view-expand-behavior`) — import { AXTreeViewExpandBehavior } from '@acorex/components/tree-view';
  Defines the behavior for expanding tree view nodes.
- **AXTreeViewLook** (`tree-view-look`) — import { AXTreeViewLook } from '@acorex/components/tree-view';
  Defines the visual appearance style for tree view components.
- **AXTreeViewSelectionBehavior** (`tree-view-selection-behavior`) — import { AXTreeViewSelectionBehavior } from '@acorex/components/tree-view';
  Defines the behavior for tree view selection interactions.
- **AXTreeViewSelectionScope** (`tree-view-selection-scope`) — import { AXTreeViewSelectionScope } from '@acorex/components/tree-view';
  Defines the scope of selection for tree view components.
- **AXUploadRequest** (`upload-request`) — import { AXUploadRequest } from '@acorex/components/uploader';
  Represents a file upload request with progress tracking, status management, and event handling.
- **AXValidationRuleOptions** (`validation-rule-options`) — import { AXValidationRuleOptions } from '@acorex/core/validation';
  Base interface for validation rule options that provides common configuration properties for all validation rules.
- **AXValidationRuleResult** (`ax-validation-ruleresult`) — import { AXValidationRuleResult } from '@acorex/core/validation';
  The AXValidationRuleResult type
- **AXValidationRuleResult** (`validation-rule-result`) — import { AXValidationRuleResult } from '@acorex/core/validation';
  Interface representing the result of a validation rule execution, containing validation status and error information.
- **AXValidationService** (`validation-service`) — import { AXValidationService } from '@acorex/core/validation';
  Service class for managing validation rules, executing validations, and providing validation results.
- **bar-chart-clustered-data** (`bar-chart-clustered-data`) — import { AXBarChartClusteredData } from '@acorex/charts/bar-chart';
- **bar-chart-messages** (`bar-chart-messages`) — import { AXBarChartMessages } from '@acorex/charts/bar-chart';
- **Bottom Navigation Look Type** (`ax-bottom-navigation-look`) — import { AXBottomNavigationLook } from '@acorex/components/bottom-navigation';
  Type definition for the visual appearance style of the bottom navigation component. This type defines the different look…
- **Calendar Cell CSS Class** (`ax-calendar-cell-css-class`) — import { AXCalendarCellCssClass } from '@acorex/components/calendar';
  Defines the type for specifying CSS classes for calendar cells, allowing custom styling of individual date cells.
- **Calendar Disabled Dates** (`ax-calendar-disabled-dates`) — import { AXCalendarDisabledDates } from '@acorex/components/calendar';
  Defines the type for specifying dates that should be disabled in calendar components, preventing user selection of those…
- **Calendar Holiday Dates** (`ax-calendar-holiday-dates`) — import { AXCalendarHolidayDates } from '@acorex/components/calendar';
  Defines the type for specifying holiday dates in calendar components, allowing special styling and behavior for these da…
- **Calendar View Depth** (`ax-calendar-view-depth`) — import { AXCalendarViewDepth } from '@acorex/components/calendar';
  Defines the depth level for calendar view selection, determining the granularity of date selection and navigation.
- **Calendar View Type** (`ax-calendar-view-type`) — import { AXCalendarViewType } from '@acorex/components/calendar';
  Defines the different view types available for calendar components, specifying how dates are displayed and organized.
- **chart-legend-item** (`chart-legend-item`) — import { AXChartLegendItem } from '@acorex/charts/chart-legend';
- **Circular Progress Mode** (`ax-circular-progress-mode`) — import { AXCircularProgressMode } from '@acorex/components/circular-progress';
  Type definition for the circular progress mode.
- **Command Menu Item** (`menu-item`) — import { CommandMenuItem } from '@acorex/components/command';
  Interface defining the structure of a command menu item used in command components and other menu-related components.
- **Component State** (`component-state`) — import { AXComponentState } from '@acorex/cdk/common';
  Defines the possible states for AX components, indicating their current validation or status condition.
- **Conversation Message** (`ax-conversation-message`) — import { AXConversationMessage } from '@acorex/components/conversation';
  Represents a message in a conversation with properties for identification, content, timing, and user information. This i…
- **Data Pager Data** (`ax-data-pager-data`) — import { AXDataPagerData } from '@acorex/components/data-pager';
  Represents the data structure containing pagination information including page details, range, and pagination metadata.
- **Data Pager Mode** (`ax-data-pager-mode`) — (see blueprint)
  Defines the display mode options for the data pager component, controlling how pagination controls are presented to the …
- **Dialog Button Item** (`ax-dialog-button-item`) — import { AXDialogButtonItem } from '@acorex/components/dialog';
  Interface defining the properties of a button item within a dialog. This interface allows customization of button appear…
- **Dialog Configuration** (`ax-dialog-config`) — import { AXDialogConfig } from '@acorex/components/dialog';
  Configuration interface for dialog appearance and behavior. This interface defines all the properties that can be used t…
- **Dialog Data** (`ax-dialog-data`) — import { AXDialogData } from '@acorex/components/dialog';
  Interface defining the data structure passed to dialog components. This interface represents the internal data format us…
- **Dialog Reference** (`ax-dialog-ref`) — import { AXDialogRef } from '@acorex/components/dialog';
  Interface providing programmatic control over a dialog instance. This interface allows developers to close dialogs progr…
- **Direction Type** (`ax-direction`) — import { AXDirection } from '@acorex/cdk/common';
  A type that specifies the direction of component layout.
- **donut-chart-messages** (`donut-chart-messages`) — import { AXDonutChartMessages } from '@acorex/charts/donut-chart';
- **Download Response** (`ax-download-response`) — import { AXDownloadResponse } from '@acorex/core/network';
  Represents the payload emitted during a download operation, including progress state, byte counts, status, and the Blob …
- **Drawer Location** (`ax-drawer-location`) — import { AXDrawerLocation } from '@acorex/cdk/drawer';
  Type definition for the location where a drawer appears on the screen. Supports RTL-aware positioning.
- **Drawer Mode** (`ax-drawer-mode`) — import { AXDrawerMode } from '@acorex/cdk/drawer';
  Type definition for the display mode of a drawer. Determines how the drawer appears relative to the main content.
- **EditorJsTool** (`editor-js-tool`) — (see blueprint)
  Configuration object describing an EditorJS tool that can be enabled in the editor.
- **Flow Chart Edges** (`ax-flow-chart-edges`) — import { AXFlowChartEdges } from '@acorex/components/flow-chart';
  Represents a connection between two nodes in the flow chart, including identifiers and endpoint anchors.
- **Flow Chart Nodes** (`ax-flow-chart-nodes`) — import { AXFlowChartNodes } from '@acorex/components/flow-chart';
  Represents a node in the flow chart with identity, type, label, and position.
- **Form Message Style** (`ax-form-message-style`) — import { AXFormMessageStyle } from '@acorex/components/form';
  Type definition for the message display style in form fields. Determines how validation and error messages are positione…
- **Form Update On** (`ax-form-update-on`) — import { AXFormUpdateOn } from '@acorex/components/form';
  Type definition for when form validation and updates should be triggered. Determines the timing of form field validation…
- **Form Validation Rule** (`form-validation-rule`) — import { AXFormValidationRule } from '@acorex/cdk/common';
  Represents a validation rule applied to a form field, including its type, status, and options.
- **gauge-chart-messages** (`gauge-chart-messages`) — import { AXGaugeChartMessages } from '@acorex/charts/gauge-chart';
- **Grid Layout Node** (`ax-grid-layout-node`) — import { AXGridLayoutNode } from '@acorex/components/grid-layout-builder';
  Represents a complete grid layout node that extends the widget interface with additional element reference. This interfa…
- **Grid Layout Options** (`ax-grid-layout-options`) — import { AXGridLayoutOptions } from '@acorex/components/grid-layout-builder';
  Configuration options for the grid layout system. This interface defines all the customizable properties that control th…
- **hierarchy-chart-margin** (`hierarchy-chart-margin`) — import { AXHierarchyChartMargin } from '@acorex/charts/hierarchy-chart';
- **hierarchy-chart-messages** (`hierarchy-chart-messages`) — import { AXHierarchyChartMessages } from '@acorex/charts/hierarchy-chart';
- **hierarchy-chart-node** (`hierarchy-chart-node`) — import { AXHierarchyChartNode } from '@acorex/charts/hierarchy-chart';
- **Hot Key Action** (`ax-hot-key-action`) — import { AXHotKeyAction } from '@acorex/cdk/common';
  Type definition for hotkey action configuration. This type allows mapping keyboard shortcuts to specific actions or func…
- **Input Mask Modes** (`ax-input-mask-modes`) — import { AXInputMaskModes } from '@acorex/cdk/input-mask';
  Defines the available input mask modes for different types of data formatting and validation.
- **Kanban Item** (`ax-kanban-item`) — import { AXKanbanItem } from '@acorex/components/kanban';
  Type for items rendered in the Kanban component.
- **Kanban Status** (`ax-kanban-status`) — import { AXKanbanStatus } from '@acorex/components/kanban';
  Type defining a Kanban status (column).
- **Label Mode** (`ax-label-mode`) — import { AXLabelMode } from '@acorex/components/form';
  Type definition for the label display mode in form fields. Determines how labels are positioned and displayed relative t…
- **Line Chart Data** (`line-chart-data`) — import { AXLineChartData } from '@acorex/charts/line-chart';
  Interface definition for line chart data series configuration
- **Line Chart Messages** (`line-chart-messages`) — import { AXLineChartMessages } from '@acorex/charts/line-chart';
  Interface for customizable messages displayed in line chart empty states
- **Line Chart Option** (`line-chart-option`) — import { AXLineChartOption } from '@acorex/charts/line-chart';
  Configuration interface for line chart component with comprehensive styling and behavior options
- **Line Chart Value** (`line-chart-value`) — import { AXLineChartValue } from '@acorex/charts/line-chart';
  Type definition for line chart data - supports both single series and array of series
- **Outline Item Type** (`outline-item-type`) — import { outlineItemType } from '@acorex/cdk/outline';
  Type definition for outline items used by the ACoreX outline navigation system. This type represents the structure of in…
- **Scheduler Appointment** (`ax-scheduler-appointment`) — import { AXSchedulerAppointment } from '@acorex/components/scheduler';
  Defines the structure of an appointment in the scheduler component.
- **Scheduler Appointment Data Source** (`ax-scheduler-appointment-data-source`) — import { AXSchedulerAppointmentDataSource } from '@acorex/components/scheduler';
  Defines the data source types for scheduler appointments.
- **Scheduler Appointment Loader** (`ax-scheduler-appointment-loader`) — import { AXSchedulerAppointmentLoader } from '@acorex/components/scheduler';
  Defines the synchronous loader function type for scheduler appointments.
- **Scheduler Appointment Loader Async** (`ax-scheduler-appointment-loader-async`) — import { AXSchedulerAppointmentLoaderAsync } from '@acorex/components/scheduler';
  Defines the asynchronous loader function type for scheduler appointments.
- **Scheduler Resource** (`ax-scheduler-resource`) — import { AXSchedulerResource } from '@acorex/components/scheduler';
  Defines the structure of a resource in the scheduler component.
- **Scheduler View Type** (`ax-scheduler-view`) — import { AXSchedulerView } from '@acorex/components/scheduler';
  Defines the available view modes for the scheduler component.
- **Selection Group Selected Keys** (`ax-selection-group-selected-keys`) — import { AXSelectionGroupSelectedKeys } from '@acorex/cdk/selection';
  Type definition for the selected keys in a selection group. This type represents either a single selected key or an arra…
- **Selection List Look Type** (`ax-selection-list-look`) — import { AXSelectionListLook } from '@acorex/components/selection-list';
  A type that specifies the visual appearance style of the selection list component.
- **Selection Mode** (`selection-mode`) — import { AXSelectionMode } from '@acorex/cdk/common';
  Type that defines how items can be selected within a component: single, multiple, or none.
- **Side Menu Item** (`ax-side-menu-item`) — import { AXSideMenuItem } from '@acorex/components/side-menu';
  Interface defining the structure of a side menu item with support for nested children, routing, and various states.
- **Side Menu Location Type** (`ax-side-menu-location`) — import { AXSideMenuLocation } from '@acorex/components/side-menu';
  Type definition for the indicator location in side menu components with line-based looks.
- **Side Menu Look Type** (`ax-side-menu-look`) — import { AXSideMenuLook } from '@acorex/components/side-menu';
  Type definition for the visual appearance style of the side menu component.
- **Time Duration Unit** (`time-duration-unit`) — import { AXTimeDurationUnit } from '@acorex/components/time-duration';
  Defines the available time units for the time duration component.
- **Upload Response** (`ax-upload-response`) — import { AXUploadResponse } from '@acorex/core/network';
  Represents the payload emitted during an upload operation, including progress state, byte counts, status, and optional r…

## events (61)

- **Action Sheet Event** (`ax-action-sheet-event`) — import { AXActionSheetEvent } from '@acorex/components/action-sheet';
  Represents the event payload when an action sheet is interacted with or closed. This interface defines the structure for…
- **AXBarChartClickEvent** (`bar-chart-click-event`) — import { AXBarChartClickEvent } from '@acorex/charts/bar-chart';
  Emitted when a bar is clicked, providing the clicked bar data and native event details
- **AXButtonClickEvent** (`button-click-event`) — import { AXButtonClickEvent } from '@acorex/cdk/common';
  Fires each time the user clicks the button element.
- **AXClickEvent** (`click-event`) — import { AXClickEvent } from '@acorex/cdk/common';
  The event emitted when the clickable element is clicked
- **AXCollapseClickEvent** (`collapse-click-event`) — (see blueprint)
  Event emitted when the collapse header is clicked. Extends AXClickEvent and adds a handled flag to optionally suppress t…
- **AXComponentCloseEvent** (`component-close-event`) — import { AXComponentCloseEvent } from '@acorex/cdk/common';
  Event emitted when a component is closed, providing component reference, HTML element, and optional data.
- **AXConversationActionEvent** (`conversation-action-event`) — import { AXConversationActionEvent } from '@acorex/components/conversation';
  Emitted when an action occurs in a conversation, providing the action data.
- **AXDropListDroppedEvent** (`drop-list-dropped-event`) — import { AXDropListDroppedEvent } from '@acorex/cdk/drag-drop';
  Event emitted when an item is dropped into a drop list, providing information about the drop operation.
- **AXDropZoneDropEvent** (`drop-zone-drop-event`) — import { AXDropZoneDropEvent } from '@acorex/cdk/drag-drop';
  Event emitted when a draggable element is dropped into a drop zone.
- **AXDropZoneHoverEvent** (`drop-zone-hover-event`) — import { AXDropZoneHoverEvent } from '@acorex/cdk/drag-drop';
  Event emitted when a draggable element enters or leaves a drop zone.
- **AXEvent** (`event`) — import { AXEvent } from '@acorex/cdk/common';
  Base event containing component context and optional DOM element.
- **AXEventData** (`event-data`) — import { AXEventData } from '@acorex/core/events';
  Data structure for AX events, containing type and optional payload.
- **AXHierarchyChartClickEvent** (`hierarchy-chart-click-event`) — import { AXHierarchyChartClickEvent } from '@acorex/charts/hierarchy-chart';
  Event interface emitted when a node in the hierarchy chart is clicked, providing access to the clicked node data, DOM ev…
- **AXHierarchyChartToggleEvent** (`hierarchy-chart-toggle-event`) — import { AXHierarchyChartToggleEvent } from '@acorex/charts/hierarchy-chart';
  Event interface emitted when a node in the hierarchy chart is expanded or collapsed, providing access to the affected no…
- **AXHtmlEvent** (`html-event`) — import { AXHtmlEvent } from '@acorex/cdk/common';
  Event that includes a native DOM event.
- **AXInputFileChange** (`input-file-change`) — import { AXInputFileChange } from '@acorex/components/conversation';
  Emitted when the input file changes, providing the new file data.
- **AXItemClickEvent** (`item-click-event`) — import { AXItemClickEvent } from '@acorex/cdk/common';
  Emitted when an item is clicked, providing the item and its name.
- **AXListScrollIndexChanged** (`ax-list-scroll-index-changed`) — (see blueprint)
  Payload emitted when the scrolled item index changes in AXListComponent.
- **AXOptionChangedEvent<T>** (`option-changed-event`) — import { AXOptionChangedEvent } from '@acorex/cdk/common';
  Event emitted when a component option changes.
- **AXOtpCompletedEvent** (`otp-completed-event`) — import { AXOtpCompletedEvent } from '@acorex/components/otp';
  Emitted when an OTP input is completed, providing the entered value and completion status.
- **AXPlatformEvent** (`platform-event`) — import { AXPlatformEvent } from '@acorex/core/platform';
  Event for platform-specific actions, containing the native event, source, and optional data.
- **AXRailClickEvent** (`ax-rail-click-event`) — import { AXRailClickEvent } from '@acorex/components/rail-navigation';
  Event emitted when a rail navigation item is clicked.
- **AXRangeChangedEvent** (`range-changed-event`) — import { AXRangeChangedEvent } from '@acorex/cdk/common';
  Emitted when a range changes, providing the start and end values.
- **AXSchedulerAppointmentMouseEvent** (`ax-scheduler-appointment-mouse-event`) — import { AXSchedulerAppointmentMouseEvent } from '@acorex/components/scheduler';
  Mouse event emitted for scheduler appointments.
- **AXSchedulerSlotDropEvent** (`ax-scheduler-slot-drop-event`) — import { AXSchedulerSlotDropEvent } from '@acorex/components/scheduler';
  Event emitted when an appointment is dropped onto a scheduler slot.
- **AXSchedulerSlotMouseEvent** (`ax-scheduler-slot-mouse-event`) — import { AXSchedulerSlotMouseEvent } from '@acorex/components/scheduler';
  Mouse event emitted for scheduler slots.
- **AXSelectionValueChangedEvent** (`selection-value-changed-event`) — import { AXSelectionValueChangedEvent } from '@acorex/cdk/common';
  Emitted when the selection value changes, providing the new and old values, selected keys, and selected items.
- **AXSideMenuItemClickEvent** (`ax-side-menu-item-click-event`) — import { AXSideMenuItemClickEvent } from '@acorex/components/side-menu';
  Click event emitted by a side menu item.
- **AXTabStripChangedEvent** (`tab-strip-changed-event`) — import { AXTabStripChangedEvent } from '@acorex/components/tabs';
  Event emitted when the active tab changes in a tab component.
- **AXTreeItemClickBaseEvent** (`tree-item-click-base-event`) — import { AXTreeItemClickBaseEvent } from '@acorex/components/tree-view';
  Event emitted when a tree item is clicked, selected, or interacted with.
- **AXUploaderChangedEvent** (`uploader-changed-event`) — import { AXUploaderChangedEvent } from '@acorex/components/uploader';
  Event fired when files are changed in the uploader (added, removed, etc.).
- **AXUploaderFileCancelEvent** (`uploader-file-cancel-event`) — import { AXUploaderFileCancelEvent } from '@acorex/components/uploader';
  Event fired when a file upload is canceled.
- **AXUploaderFileCompleteEvent** (`uploader-file-complete-event`) — import { AXUploaderFileCompleteEvent } from '@acorex/components/uploader';
  Event fired when a single file upload is completed.
- **AXUploaderFilesCompleteEvent** (`uploader-files-complete-event`) — import { AXUploaderFilesCompleteEvent } from '@acorex/components/uploader';
  Event fired when all files upload is completed.
- **AXUploaderFileStartEvent** (`uploader-file-start-event`) — import { AXUploaderFileStartEvent } from '@acorex/components/uploader';
  Event fired when a file upload starts.
- **AXValueChangedEvent<T>** (`value-changed-event`) — import { AXValueChangedEvent } from '@acorex/cdk/common';
  Emitted when a value changes, providing the new and old values as well as the name of the value.
- **Calendar Navigate Event** (`ax-calendar-navigate-event`) — import { AXCalendarNavigateEvent } from '@acorex/components/calendar';
  Represents the event payload when navigation occurs in a calendar component, such as when moving between months or years…
- **Calendar Slot Click Event** (`ax-calendar-slot-click`) — import { AXCalendarSlotClick } from '@acorex/components/calendar';
  Represents the event payload when a calendar slot (date cell) is clicked by the user.
- **Column Size Changed Event** (`on-column-size-changed-event`) — (see blueprint)
  Represents the event payload when the size of columns in a data table changes.
- **Columns Order Changed Event** (`ax-columns-order-changed-event`) — (see blueprint)
  Represents the event payload when the order of columns in a data table changes.
- **Command Component Event** (`ax-command-component-event`) — import { AXCommandComponentEvent } from '@acorex/components/command';
  Represents the event payload when a command component item is selected or submitted.
- **Context Menu Items Click Event** (`ax-context-menu-items-click-event`) — import { AXContextMenuItemsClickEvent } from '@acorex/components/menu';
  Represents the event when a context-menu item is clicked. Extends NXClickEvent and carries the clicked item's payload an…
- **Context Menu Opening Event** (`ax-context-menu-opening-event`) — import { AXContextMenuOpeningEvent } from '@acorex/components/menu';
  Represents the event payload when a context menu is about to open. This event allows you to modify the menu items or can…
- **Conversation Menu Opening Event** (`conversation-menu-opening-event`) — import { AXConversationMenuOpeningEvent } from '@acorex/components/conversation';
  Represents the event payload when a conversation action menu is about to open. This event allows you to modify the menu …
- **Data Pager Changed Event** (`data-pager-changed-event`) — import { AXDataPagerChangedEvent } from '@acorex/components/data-pager';
  Represents the event emitted when the data pager state changes, providing information about the current page, range, and…
- **Data Table Row Click Event** (`ax-data-table-row-click`) — (see blueprint)
  Represents the event payload when a row in the data table is clicked.
- **Data Table Row Double Click Event** (`ax-data-table-row-db-click`) — (see blueprint)
  Represents the event payload when a row in the data table is double-clicked.
- **Drop Zone Drop Event** (`ax-drop-zone-drop-event`) — import { AXDropZoneDropEvent } from '@acorex/cdk/drag-drop';
  Event emitted when an element is dropped onto a drop zone.
- **Focus Event** (`focus-event`) — import { AXFocusEvent } from '@acorex/cdk/common';
  Represents the event payload emitted when a component gains or loses focus.
- **Form Validation Event** (`ax-form-validation-event`) — import { AXFormValidationEvent } from '@acorex/components/form';
  Event emitted when form validation is performed. Contains the validation results and component context.
- **Grid Layout Event** (`ax-grid-layout-event`) — import { AXGridLayoutEvent } from '@acorex/components/grid-layout-builder';
  Event payload emitted by the grid layout container when widgets are added, removed, or changed. This event provides info…
- **Input Mask Event** (`ax-input-mask-event`) — import { AXInputMaskEvent } from '@acorex/cdk/input-mask';
  Represents the event payload when an input mask value changes, providing both masked and unmasked values along with comp…
- **Kanban Mouse Event** (`ax-kanban-mouse-event`) — import { AXKanbanMouseEvent } from '@acorex/components/kanban';
  Represents the payload for Kanban item mouse interactions (click, double-click, right-click, action click).
- **Kanban Sort Event** (`ax-kanban-sort-event`) — import { AXKanbanSortEvent } from '@acorex/components/kanban';
  Represents the payload emitted when an item is dropped to reorder within a status or moved across statuses in the Kanban…
- **KeyboardEvent** (`keyboard-event`) — // Standard DOM event, no import required for usage in TypeScript/JavaScript.
  Standard DOM event representing keyboard input, providing information about the key pressed, modifier states, and the ta…
- **Line Chart Point Click Event** (`line-chart-point-click-event`) — import { AXLineChartPointClickEvent } from '@acorex/charts/line-chart';
  Event interface emitted when a data point is clicked in the line chart
- **Menu Item Click Event** (`ax-menu-item-click-event`) — import { AXMenuItemClickEvent } from '@acorex/components/menu';
  Payload emitted when a menu item component is clicked.
- **Menu Items Click Event** (`ax-menu-items-click-event`) — import { AXMenuItemsClickEvent } from '@acorex/components/menu';
  Payload emitted when a menu item is clicked on the root menu.
- **NXClickEvent** (`nx-click-event`) — import { NXClickEvent } from '@acorex/cdk/common';
  Base click event class for NX components that extends NXNativeEvent with MouseEvent.
- **NXEvent** (`nx-event`) — import { NXEvent } from '@acorex/cdk/common';
  Base event class for NX components.
- **NXNativeEvent** (`nx-native-event`) — import { NXNativeEvent } from '@acorex/cdk/common';
  Contains native event information for NX components.

## styles (5)


## tokens (1)


## get-started (4)

